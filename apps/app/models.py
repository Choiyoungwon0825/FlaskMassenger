# from apps.app.models import db
# from datetime import datetime

# class Chat(db.Model):
#     __tablename__ = "chat"
#     id = db.Column(db.Integer, primary_key=True)
#     chat = db.Column(db.String, index=True)
#     create = db.Column(db.DateTime, default = datetime.now)